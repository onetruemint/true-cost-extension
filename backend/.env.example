# Server Configuration
PORT=3000

# Supabase Configuration
# Get these from your Supabase project dashboard: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Optional: Service key for admin operations (keep secret!)
# SUPABASE_SERVICE_KEY=your-service-key

# Optional: Specific origin to allow (in addition to browser extensions)
# ALLOWED_ORIGIN=http://localhost:3001

# For Google OAuth (configure in Supabase dashboard)
# The redirect URL should point to your extension's auth.html
